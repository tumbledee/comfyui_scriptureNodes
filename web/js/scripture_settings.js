/**
 * Calibrate inscriptions holds setting data
 * 
 * Backup Data logic settings (for Checkpoint data)
 * - Allow backup Checkmark (standard checked)
 * - Backup method: during start, before save, once per day, once per week, once per month
 * - Write method: OVerwrite single file, write to new file with timestamp
 * - retrieve from backup (select backup file)
 */